<%= form_with(model: tournament_player) do |form| %>
  <% if tournament_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournament_player.errors.count, "error") %> prohibited this tournament_player from being saved:</h2>

      <ul>
        <% tournament_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tournament_id %>
    <%= form.text_field :tournament_id %>
  </div>

  <div class="field">
    <%= form.label :player_id %>
    <%= form.text_field :player_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
